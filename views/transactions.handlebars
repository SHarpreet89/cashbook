<main class="container">
    <section class="row">
        <article class="col-12">
            <div class="card">
                <h2>Add Transaction</h2>
                <form id="add-transaction">
                    <div>
                        <label for="transaction-name">Name:</label>
                        <input type="text" id="transaction-name" placeholder="Enter transaction name">
                    </div>
                    <div>
                        <label for="transaction-amount">Amount:</label>
                        <input type="number" id="transaction-amount" placeholder="Enter transaction amount">
                    </div>
                    <div>
                        <label for="transaction-category">Category:</label>
                        <select id="transaction-category">
                            <option value="">Loading categories...</option> <!-- Placeholder until categories are fetched -->
                        </select>
                    </div>
                </form>
            </div>
        </article>
    </section>
    
    <section class="row">
        <article class="col-12">
            <div id="submit-btn" class="flex">
                <button type="submit" id="submit-transaction" value="Submit Transaction">Submit</button>
            </div>
        </article>
    </section>

    <section>
        <div class="card">
            <h2>Transaction History</h2>
            <ul>
                {{#each transaction}}
                <li>
                    <section class="row">
                        <article class="col-6">
                            <p>Name: {{transaction.name}}</p>
                            <p>Category: {{transaction.category}}</p>
                        </article>
                        <article class="col-6">
                            <p>Amount: {{transaction.amount}}</p>
                            <p>Date: {{transaction.date}}</p>
                        </article>
                    </section>
                </li>
                {{/each}}
            </ul>
        </div>
    </section>
    <script src="/js/transaction.js"></script>
</main>
